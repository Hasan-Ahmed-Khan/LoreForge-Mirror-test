# The manuscript

Generated from `canon/`, one file per chapter, scenes in reading order.

**Editing these files changes nothing.** They are re-rendered on every commit
and overwritten; LoreForge never reads them back. The prose itself lives in
`canon/scenes/<uuid>.md`, and that is what a merge resolves.
